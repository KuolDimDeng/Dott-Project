// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#89CFF0', // Baby blue color
    },
    babyBlue: {
      main: '#89CFF0',
      dark: '#7AB8D9',
    },
  },
});

export default theme;