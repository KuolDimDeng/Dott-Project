// Safe loader for Cloudflare analytics that handles errors gracefully
export function loadCloudflareAnalytics() {
  // Temporarily disabled due to integrity check failures and tracking protection
  // Will be re-enabled once Cloudflare configuration is updated
  return;
}
