import { Setup } from '@/app/onboarding/components/steps/Setup/Setup';

export const metadata = {
  title: 'Setup - Onboarding',
  description: 'Setting up your account',
};

export default function SetupPage() {
  return <Setup />;
}
