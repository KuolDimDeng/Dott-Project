export const metadata = {
  title: 'Payment - Onboarding | PyFactor',
  description: 'Complete your payment information to continue with PyFactor setup',
};

export default function PaymentLayout({ children }) {
  return children;
}
