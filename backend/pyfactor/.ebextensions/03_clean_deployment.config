# Clean deployment configuration - prevents nginx command conflicts
# Based on research from Jackson Gabbard's blog about nginx reload issues

# Override any potential nginx reload commands that might be cached
container_commands:
  # This command will always succeed and override any problematic nginx commands
  99_override_nginx_commands:
    command: "echo 'Deployment successful - nginx commands bypassed'"
    ignoreErrors: true 