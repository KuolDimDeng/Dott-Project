version: '3'
services:
  app:
    build:
      context: .
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "8081:8081"
    command: npm start
    environment:
      - NODE_ENV=development
